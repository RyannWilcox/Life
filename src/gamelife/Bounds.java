package gamelife;

public interface Bounds {

    /*
     * Bounds used for checking the neighbors of each square on the grid
     */
    static final int MAX_BOUND = 100;
    static final int MIN_BOUND = 0;

    /*
    * Max number of rows and columns
     */
    static final int MAX_ROWS = 100;
    static final int MAX_COLUMNS = 100;

}
